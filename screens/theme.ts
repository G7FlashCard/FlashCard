// FlashLearn design tokens (sampled from the mockups).
// Reuse these on every screen so the app stays consistent.

export const colors = {
  primary: '#5B7BE8',
  primaryPressed: '#4A68D2',
  primarySoft: '#DDE4FB',
  primaryMid: '#C7D2FE',
  primaryTint: '#EEF2FF',
  navy: '#1E2A5A',
  ink: '#111936',
  body: '#6B7280',
  border: '#E5E7EB',
  background: '#FFFFFF',
  skin: '#F2C4A8',
  hair: '#1B1B2F',
  success: '#22C55E',
  flame: '#FB923C',
  flameSoft: '#FFF1E0',
} as const;

export const radius = {
  sm: 8,
  md: 12,
  lg: 16,
  pill: 999,
} as const;

export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
} as const;